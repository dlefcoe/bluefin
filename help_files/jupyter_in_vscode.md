# shortcuts for python notebook

Below are some of the main shortcuds when using a `jupyter notwbook` inside of `VScode`.

  - add block before = a
  - add block after = b
  - copy a block = c
  - delete a block of code = dd
  - undo last move = ctrl+z
  - markdown = m
  - code = y


